enum DialogType { confirmation, information, warning }
enum ApiResult { loading, success, error, canceled }

class Screens {
  static const String preparations = '/preparations';
  static const String preparationDetail = '/preparationDetail';
}
